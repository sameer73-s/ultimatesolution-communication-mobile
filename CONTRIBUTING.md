# سياسة المساهمة

يُدار المشروع ببوابات اعتماد متسلسلة. فرع `main` محمي ولا يُدفع إليه مباشرة. لكل خطوة فرعية من WBS، أنشئ فرعًا مستقلًا بالصيغة `feature/<WBS-ID>-<short-name>`، مثل `feature/3.1-backend-foundation`.

يجب أن ينتهي كل نطاق عمل بطلب سحب يستخدم قالب `.github/PULL_REQUEST_TEMPLATE.md`. يُفتح طلب السحب للمراجعة بعد التحقق والتوثيق، ولا يُدمج بواسطة Manus. يمثل الدمج بعد اعتماد صريح من الجهة المخولة بوابة بدء الخطوة التالية.

يجب أن تطابق الحقول الخاصة برقم WBS والمخرج المتوقع ومعيار القبول النص المعتمد في ملف إدارة المشروع. لا تُضاف حزم أو خدمات خارجية أو تغييرات معمارية خارج الدليل المعتمد دون موافقة صريحة.
